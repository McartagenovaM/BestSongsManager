@model SongManager.Models.Song
@{
    ViewData["Title"] = Model.Title;
}

<div class="row page-header">
    <div class="col-12">
        <h1>@Model.Title</h1>
        <p class="lead text-muted">by @Model.Artist</p>
    </div>
</div>

<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-body">
                <div class="row song-details">
                    <div class="col-md-6">
                        <h6 class="detail-label">Album</h6>
                        <p>@Model.Album</p>
                    </div>
                    <div class="col-md-6">
                        <h6 class="detail-label">Year</h6>
                        <p>@Model.Year</p>
                    </div>
                </div>
                <div class="row song-details">
                    <div class="col-md-6">
                        <h6 class="detail-label">Genre</h6>
                        <p>@Model.Genre</p>
                    </div>
                    <div class="col-md-6">
                        <h6 class="detail-label">Favorite</h6>
                        <p>
                            @if (Model.IsFavorite)
                            {
                                <span class="badge bg-danger favorite-badge">Yes ★</span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">No</span>
                            }
                        </p>
                    </div>
                </div>
                @if (!string.IsNullOrEmpty(Model.Notes))
                {
                    <div class="song-details">
                        <h6 class="detail-label">Notes</h6>
                        <p>@Model.Notes</p>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

<div class="action-buttons back-button">
    <a href="/Songs/Edit/@Model.Id" class="btn btn-warning">Edit</a>
    <form method="post" action="/Songs/ToggleFavorite/@Model.Id" class="form-inline">
        <button type="submit" class="btn @(Model.IsFavorite ? "btn-danger" : "btn-success")">
            @(Model.IsFavorite ? "Remove from Favorites" : "Add to Favorites")
        </button>
    </form>
    <form method="post" action="/Songs/Delete/@Model.Id" class="form-inline">
        <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this song?')">Delete</button>
    </form>
    <a href="/Songs" class="btn btn-secondary">Back to Songs</a>
</div>
